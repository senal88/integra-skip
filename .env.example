# Current state
# - This frontend-only project builds without mandatory environment variables.
# - Copy only the variables you actually enable from ~/.env.global.
# - Never commit secrets or service-role keys.

# Deployment metadata
PROJECT_ENV=development
DEPLOY_ENV=development
DOMAIN=
SITE_URL=
PORT=5173

# Optional client-side integration surface
# Enable only when the code starts consuming these values.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
